import { observer, inject } from 'mobx-react';
import React from 'react';

@observer
class Home extends React.Component {
  render() {
    return (
      <div>
        <p>Home</p>
      </div>
    );
  }
}

export default Home;
